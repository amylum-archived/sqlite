name 'sqlite'
org 'amylum'

source(
  type: 'tar',
  url: 'https://www.sqlite.org/2017/sqlite-autoconf-3170000.tar.gz'
)

configure_flags(
  prefix: '/usr'
)

harden

patch 'license.patch'

build do
  configure
  make
  install
end

test do
  # TODO: add tests
end
